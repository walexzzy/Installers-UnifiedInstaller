<parameters>
  
  <parameter 
    name="ApplicationPath" 
    description="Full site path where you would like to install your application (i.e., PloneIISApp)" 
    defaultValue="Default Web Site/PloneIISApp" 
    tags="iisApp">
    <parameterEntry kind="ProviderPath" scope="iisApp" match="PloneIISApp" />
  </parameter>

  <parameter 
      name="password"
      friendlyName="Admin password"
      description="Enter a password for the 'admin' user."
      tags="New,Password">
    <!-- At least 5 characters -->
    <parameterValidation type="RegularExpression" validationString=".{5,}" />
    <parameterEntry kind="TextFile" scope="\\setup\.py$"
                    match="__msdeploy_password_parameter__" />
  </parameter>

  <parameter 
      name="zeo_port"
      friendlyName="ZEO Port"
      description="If set, a ZEO service will be installed and the Zope instance(s) will connect to it.  If the value is an integer, the ZEO service will listen on that port.  If the value is 'auto' then the last digits of '8100' will be replaced by the number suffix of the installed app. If not set, a standalon Zope instance will be created."
      defaultValue="auto">
    <parameterValidation type="RegularExpression,AllowEmpty" validationString="auto|[0-9]{0,5}" />
    <parameterEntry kind="TextFile" scope="\\setup\.py$"
                    match="__msdeploy_zeo_port_parameter__" />
  </parameter>
  
  <parameter 
      name="clients"
      friendlyName="Zope client count"
      description="If 'ZEO Port' is set, this specifies the number of Zope clients IIS may launch.  If unset, the default is the number of processors unless 'Development Tools' is 'True', then the default is 1 for easier debugging."
      defaultValue="">
    <!-- Integer >= 1 -->
    <parameterValidation type="RegularExpression,AllowEmpty" validationString="[1-9][0-9]*" />
    <parameterEntry kind="TextFile" scope="\\setup\.py$"
                    match="__msdeploy_clients_parameter__" />
  </parameter>

  <parameter 
      name="develop"
      friendlyName="Development Tools?"
      description="If `True`, include include various development tools."
      defaultValue="True">
    <parameterValidation type="Boolean" />
    <parameterEntry kind="TextFile" scope="\\setup\.py$"
                    match="__msdeploy_develop_parameter__" />
  </parameter>
  
</parameters>
